#ifndef DRAW_H
#define DRAW_H

void draw_menu(const int& screen_width, const int& screen_height, bool& is_playing);
void draw_game(const int& screen_width, const int& screen_height);
void draw(const int& width, const int& height, bool& is_playing);

#endif // DRAW_H